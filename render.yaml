services:
  - type: web
    name: RAJPUROHIT 
    env: python
    buildCommand: |
      apt-get update -y && apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: python bot.py
