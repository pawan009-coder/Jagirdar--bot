services:
  - type: web
    name: Jagirdar--bot
    env: python
    buildCommand: |
      apt-get update -y && apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: python bot.py
